# JexFold

[English](README.md) · **Русский**

JexFold — локальное приложение для обратимой перепаковки JPEG в JPEG XL. Оно работает как desktop-приложение и как статическое web-приложение; текущая версия — **0.0.3**.

Приложение использует официальные `cjxl` и `djxl`, работает локально и после каждого сжатия восстанавливает исходный JPEG и сравнивает его побайтно. Оригиналы не удаляются, а существующие результаты не перезаписываются.

## Возможности

- сжатие JPEG → JXL и обычная конвертация произвольных JXL → JPEG;
- выбор файлов и папок, drag-and-drop и рекурсивный поиск `.jpg`/`.jpeg`;
- сохранение структуры исходных папок в отдельном каталоге назначения;
- строгая проверка JPEG → JXL → JPEG и вычисление SHA-256 исходного файла;
- пауза, отмена, настройка параллельной нагрузки и очередь с пагинацией;
- возможность не сохранять JXL, если он не меньше исходного JPEG;
- интерфейс на английском и русском, определение языка системы и сохранение выбора;
- локальная обработка без сетевых запросов во время преобразования.

Приложение не меняет качество, размеры или встроенные метаданные JPEG.

## Установка

### Web

Статическая web-версия публикуется отдельным workflow **Web / GitHub Pages**.
Она запускает `libjxl.wasm` в Web Workers и никуда не загружает фотографии.
Chrome и Edge могут записывать дерево папок прямо в выбранный каталог; в других
браузерах готовые файлы скачиваются обычным способом. После первой полной
загрузки Service Worker позволяет запускать приложение офлайн.

Владельцу репозитория нужно один раз выбрать **GitHub Actions** как источник
Pages в настройках GitHub.

### Desktop

Готовые пакеты публикуются на странице Releases репозитория:

- Windows 10/11 — установщик NSIS (`.exe`);
- macOS 11+ — образ диска (`.dmg`);
- Debian/Ubuntu — пакет `.deb`; другие Linux-дистрибутивы — `.AppImage`.

Версия 0.0.3 не подписана. Windows SmartScreen и macOS Gatekeeper могут показать предупреждение о неизвестном издателе. Проверяйте загрузку по файлу `SHA256SUMS`, приложенному к каждому релизу.

## Использование

1. Выберите JPEG/JXL-файлы или исходную папку.
2. Выберите отдельную папку назначения. Она не должна быть вложена в исходную папку, а исходная — в неё.
3. Выберите режим преобразования и параметры обработки.
4. Нажмите **«Начать сжатие»** или **«Конвертировать в JPEG»**.
5. Проверьте результат в приложении.

Пример размещения результата:

```text
Исходник:   /photos/2026/IMG_001.JPG
Назначение: /archive
Результат:  /archive/photos/2026/IMG_001.jxl
```

Расширение JPEG заменяется на `.jxl`. Если в одной папке есть одноимённые файлы `.jpg` и `.jpeg`, они соответствуют одному результату: первый будет создан, а второй получит статус `existing`. Этот статус означает только, что файл уже существовал и не был перезаписан; его содержимое при повторном запуске не проверяется.

### Язык интерфейса

Интерфейс поддерживает английский и русский. Настройка **«Системный»** использует язык операционной системы/браузера и переключается на английский, если этот язык не поддерживается. Язык, выбранный вручную, сохраняется локально.

Переводы находятся в `src/i18n/locales/` и используют стабильные семантические идентификаторы, поэтому новые языки можно добавлять без изменения ядра конвертации.

## Важные ограничения

- Каталог назначения должен находиться на файловой системе с поддержкой жёстких ссылок, например NTFS, APFS или ext4. FAT/exFAT не поддерживаются; поведение SMB/NFS зависит от реализации.
- Для каждого активного задания временно требуется место под JPEG, JXL и восстановленный JPEG.
- Встроенные EXIF, ICC, IPTC и XMP сохраняются как часть исходного JPEG и проверяются побайтным восстановлением. Desktop-версия также переносит даты изменения и доступа, POSIX-права в macOS/Linux и, в Windows, исходную дату создания. ACL, Windows-атрибуты, Finder-теги, расширенные атрибуты и отдельные XMP/AAE-файлы не переносятся.
- Символические ссылки не обходятся. Максимум очереди — 250 000 файлов.
- До самостоятельного удаления оригиналов проверьте резервную копию и восстановление нескольких характерных файлов.

## Запуск из исходников

Требуются Node.js 22.12+, Rust 1.88, Git, CMake и C/C++ toolchain. Установите [системные зависимости Tauri 2](https://v2.tauri.app/start/prerequisites/), затем выполните:

```bash
pnpm install --frozen-lockfile
pnpm run codecs:build
pnpm run tauri -- dev
```

`codecs:build` загружает исходный код официального libjxl v0.12.0, проверяет ожидаемый commit, собирает `cjxl` и `djxl` для текущей ОС и архитектуры и кладёт их в `src-tauri/resources/codecs/`. Кодеки одной платформы нельзя включать в пакет другой платформы.

Для Ubuntu 22.04/24.04 нужны дополнительные пакеты:

```bash
sudo apt-get update
sudo apt-get install -y build-essential git cmake pkg-config \
  libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf
```

На Windows используются MSVC, Visual Studio Build Tools и Windows SDK. На macOS нужны Xcode Command Line Tools; минимальная версия системы в конфигурации — 11.0.

## Проверки и сборка

Команды линтинга и форматирования хранятся в репозитории, поэтому локальные проверки совпадают с CI:

```bash
pnpm run lint:web
pnpm run typecheck
pnpm run format:check
pnpm run lint:rust
pnpm run format:cpp:check
```

`pnpm run lint` дополнительно запускает Clippy, clang-tidy, ShellCheck и actionlint. Для нативных команд нужны соответствующие инструменты; `lint:cpp` ожидает зафиксированный checkout libjxl после `pnpm run codecs:build`.

```bash
pnpm run check
pnpm run wasm:build
pnpm run web:build
cargo test -p jxl-core --locked
cargo clippy -p jxl-core --all-targets --locked -- -D warnings
pnpm run codecs:build
pnpm run test:integration
pnpm run desktop:build
pnpm run desktop:portable
```

Установщики создаются в `target/release/bundle/`. На Windows команда `desktop:portable` создаёт там же ZIP-архив, который можно распаковать в любую папку и запустить через `JexFold.exe` без установки. Папку `codecs` внутри архива нужно хранить рядом с EXE. Интеграционная проверка запускает настоящие `cjxl` и `djxl` на обычном и progressive JPEG и требует точной реконструкции. Полный чек-лист находится в [docs/TESTING.md](docs/TESTING.md).

Для полной локальной сборки релиза используйте отдельную цель на соответствующей ОС:

```bash
make release-windows  # NSIS-установщик и portable ZIP
make release-macos    # DMG
make release-linux    # DEB и AppImage
```

Каждая цель устанавливает зафиксированные зависимости, запускает проверки, собирает платформенные кодеки и пакеты. Результаты находятся в `target/release/bundle/`; `target/`, кодеки и `release-assets/` исключены из Git.

Если проверка сообщает об отсутствии Cargo в Windows, установите Rust и перезапустите терминал перед повторной сборкой:

```powershell
winget install --id Rustlang.Rustup -e
```

## Ручной релиз

Релизы намеренно запускаются вручную:

1. Обновите версии в `package.json`, обоих Cargo-манифестах и `src-tauri/tauri.conf.json`, затем добавьте секцию в `CHANGELOG.md`.
2. Убедитесь, что изменения находятся в ветке `main` и CI проходит.
3. Откройте **Actions → Release → Run workflow**.
4. Укажите версию без префикса `v`, например `0.0.3`.
5. Workflow проверит версии и теги, соберёт пакеты на Windows, macOS и Linux, сформирует `SHA256SUMS`, создаст тег и опубликует GitHub Release.

Публикация остановится при несовпадении версий, запуске не из `main`, существующем теге, пустых release notes или отсутствии установщика. Windows- и macOS-пакеты пока не подписываются.

## Структура проекта

```text
crates/jxl-core/        независимое Rust-ядро сканирования и конвертации
src-tauri/              desktop shell, команды Tauri и конфигурация пакетов
src/                    интерфейс React/TypeScript и общий слой i18n
scripts/                сборка и проверка официальных кодеков
tests/                  конфигурационные, форматные и локализационные тесты
.github/workflows/      CI и ручная публикация релиза
docs/                   архитектура, безопасность и тестовый чек-лист
```

Дополнительные документы: [архитектура](docs/ARCHITECTURE.md), [границы безопасности](docs/SECURITY.md), [история изменений](CHANGELOG.md) и [лицензии зависимостей](THIRD_PARTY.md).

## Лицензия

Собственный код распространяется по лицензии [MIT](LICENSE). JPEG XL и связанные компоненты сохраняют собственные лицензии; их тексты включаются в релизные пакеты.
